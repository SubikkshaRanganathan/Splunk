RewriteEngine On

# Specific redirection rule
RewriteCond %{REQUEST_URI} ^/index/?$ [NC]
RewriteRule ^index/?$ /home [R=301,L]

# General URL rewriting rule
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([^/]+)$ $1.html [L]
